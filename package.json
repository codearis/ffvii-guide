{
  "name": "ff7-psx",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cd dist && rm -rf .git && git init -b gh-pages && git add -A && git -c user.name=codearis -c user.email=codearis@users.noreply.github.com commit -m deploy && git -c credential.helper= -c 'credential.helper=!gh auth git-credential' push -f https://github.com/codearis/ffvii-guide.git gh-pages && cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
